import { DesignCompare } from "./design-compare";

export const metadata = {
  title: "Design directions — Guardian Recipe Finder",
  description: "Three type pairings against the current one, on real recipes.",
};

export default function DesignPage() {
  return <DesignCompare />;
}
